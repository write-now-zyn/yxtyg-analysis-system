# 整体看板功能测试文档

## 1. 测试环境

- 后端服务：http://localhost:8080
- 前端服务：http://localhost:8081

## 2. 功能测试用例

### 2.1 页面访问测试

| 用例ID | 测试项 | 操作步骤 | 预期结果 |
|--------|--------|----------|----------|
| TC001 | 首页跳转 | 访问 http://localhost:8081 | 自动跳转到看板页面 |
| TC002 | 菜单导航 | 点击侧边栏"整体看板" | 跳转到看板页面，菜单高亮 |

### 2.2 数据加载测试

| 用例ID | 测试项 | 操作步骤 | 预期结果 |
|--------|--------|----------|----------|
| TC003 | 默认加载 | 进入看板页面 | 显示当前月数据，月份选择器显示当前月 |
| TC004 | 切换月份 | 点击月份选择器，选择其他月份 | 数据刷新为新月份的统计数据 |
| TC005 | 无数据月份 | 选择没有数据的月份 | 显示0条记录，未达标地市列表为全部地市 |

### 2.3 总览卡片测试

| 用例ID | 测试项 | 预期结果 |
|--------|--------|----------|
| TC006 | 达标数量显示 | 显示"达标：X / 共：Y个地市"格式正确 |
| TC007 | 未达标地市显示 | 未达标地市以红色字体显示，格式为"地市名(次数)" |
| TC008 | 全部达标 | 当所有地市都达标时，显示"全部达标！"标签 |

### 2.4 排行榜测试

| 用例ID | 测试项 | 预期结果 |
|--------|--------|----------|
| TC009 | 排序正确 | 按数量降序排列 |
| TC010 | 达标标签 | 达标地市显示绿色"达标"标签 |
| TC011 | 未达标标签 | 未达标地市显示红色"未达标"标签 |

## 3. API接口测试

### 3.1 获取看板数据

**请求**
```bash
curl -X GET "http://localhost:8080/api/dashboard/overview?month=202603"
```

**预期响应**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "month": "202603",
    "statistics": {
      "demand": {
        "standard": 6,
        "reachedCount": 8,
        "totalCount": 13,
        "unreachedCities": [...],
        "ranking": [...]
      },
      "review": {...},
      "training": {...}
    }
  }
}
```

### 3.2 默认月份测试

**请求**
```bash
curl -X GET "http://localhost:8080/api/dashboard/overview"
```

**预期响应**：返回当前月份数据

## 4. 边界测试

| 用例ID | 测试场景 | 预期结果 |
|--------|----------|----------|
| TC012 | 空数据库 | 所有地市显示0次，全部未达标 |
| TC013 | 单一地市有数据 | 该地市排名第一，其他地市显示0次 |
| TC014 | participants字段为空 | 不影响统计，跳过该会议 |

## 5. 性能测试

| 测试项 | 测试方法 | 预期结果 |
|--------|----------|----------|
| 页面加载时间 | 首次加载页面 | < 2秒 |
| 月份切换响应 | 切换月份 | < 1秒 |

## 6. 兼容性测试

- Chrome浏览器：正常
- Edge浏览器：正常
- Firefox浏览器：正常

## 7. 测试结论

- [ ] 所有功能测试通过
- [ ] API接口测试通过
- [ ] 边界测试通过
- [ ] 性能测试通过

**测试人员签字**：__________

**测试日期**：__________
