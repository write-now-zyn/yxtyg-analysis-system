<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jscm.yxtyg.mapper.DashboardMapper">

    <!-- 统计某月各地市需求提单数量（按order_no前6位筛选） -->
    <select id="countDemandByCity" resultType="java.util.Map">
        SELECT 
            city,
            COUNT(*) as count
        FROM t_work_order
        WHERE LEFT(order_no, 6) = #{month}
        GROUP BY city
        ORDER BY count DESC
    </select>

    <!-- 获取所有地市列表 -->
    <select id="getAllCities" resultType="java.lang.String">
        SELECT DISTINCT city 
        FROM t_experimenter 
        WHERE city IS NOT NULL AND city != ''
        ORDER BY city
    </select>

    <!-- 统计某月各地市培训次数（累加recordCount） -->
    <select id="countTrainingByCity" resultType="java.util.Map">
        SELECT 
            city,
            SUM(record_count) as count
        FROM t_training_report
        WHERE report_month = #{month}
        GROUP BY city
        ORDER BY count DESC
    </select>

</mapper>
